{
  "name": "yoga-sequencer",
  "version": "1.0.0",
  "description": "Phone-friendly yoga class-plan generator: turns a few class parameters (including an optional peak pose) into a sequenced, safe class plan.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}
